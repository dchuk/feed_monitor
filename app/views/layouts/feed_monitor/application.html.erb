<!DOCTYPE html>
<html>
<head>
  <title><%= content_for?(:title) ? yield(:title) : "Feed Monitor" %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= turbo_include_tags %>
  <%= javascript_include_tag "stimulus.umd.js", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "feed_monitor/dropdown_transition_shim", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "stimulus-dropdown.umd.js", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "feed_monitor/controllers/notification_controller", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "feed_monitor/controllers/async_submit_controller", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "feed_monitor/controllers/dropdown_controller", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "feed_monitor/application", "data-turbo-track": "reload" %>
</head>
<body class="fm-admin">
  <%= turbo_stream_from "feed_monitor_notifications" %>
  <div class="pointer-events-none fixed inset-x-0 top-4 z-50 flex justify-end px-6">
    <div id="feed_monitor_notifications" class="flex w-full max-w-sm flex-col gap-3"></div>
  </div>
  <div class="flex min-h-screen flex-col">
    <header class="bg-slate-800 text-white shadow">
      <div class="mx-auto flex w-full max-w-6xl items-center justify-between px-6 py-4">
        <div class="text-lg font-semibold tracking-tight">Feed Monitor</div>
        <% nav_items = [
          ["Dashboard", feed_monitor.dashboard_path],
          ["Sources", feed_monitor.sources_path],
          ["Items", feed_monitor.items_path],
          ["Fetch Logs", feed_monitor.fetch_logs_path],
          ["Scrape Logs", feed_monitor.scrape_logs_path]
        ] %>
        <nav class="flex gap-2">
          <% nav_items.each do |label, path| %>
            <% active = current_page?(path) %>
            <%= link_to label, path, class: [
              "fm-admin-nav-link",
              active ? "fm-admin-nav-link--active" : "fm-admin-nav-link--inactive"
            ].join(" ") %>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="mx-auto flex w-full max-w-6xl flex-1 flex-col px-6 py-8">
      <%= yield %>
    </main>

    <footer class="bg-slate-900 py-4">
      <div class="mx-auto flex w-full max-w-6xl justify-between px-6 text-xs text-slate-300">
        <span>&copy; <%= Time.current.year %> Feed Monitor</span>
        <span>v<%= FeedMonitor::VERSION %></span>
      </div>
    </footer>
  </div>
</body>
</html>
